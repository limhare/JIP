# JIP (Just Inst Player) 기능 설명서

## 개요
JIP는 .NET MAUI 9.0 기반 안드로이드 음악 플레이어 앱으로, 악기 연습과 음악 분석을 위한 고급 오디오 기능을 제공한다.

- 패키지명: `com.instplayer.app`
- 최소 Android: 7.0 (API 24)
- 아키텍처: MVVM (CommunityToolkit.Mvvm)

---

## 1. 오디오 재생

ExoPlayer (AndroidX Media3) 기반 오디오 재생 엔진.

- **지원 포맷:** MP3, WAV, FLAC, OGG, M4A, AAC
- **지원 소스:** 로컬 파일, Content URI (`content://`), 앱 내부 다운로드 파일
- **재생 컨트롤:** 재생/일시정지, 정지, 이전 트랙, 다음 트랙
- **탐색:** 프로그레스 슬라이더 드래그로 원하는 위치 이동

---

## 2. 실시간 피치/템포 조절

SoundTouch 네이티브 라이브러리(`libsoundtouch4c.so`)를 ExoPlayer AudioProcessor로 통합.

- **피치:** -8 ~ +8 반음 (버튼 또는 슬라이더)
- **템포:** 0% ~ 200% (버튼 또는 슬라이더)
- **RST 버튼:** 각각 원래 값으로 초기화
- 재생 중 실시간 변경 가능, 설정값은 앱 종료 시 자동 저장

---

## 3. A-B 구간 반복

특정 구간을 반복 재생하여 연습에 활용.

- **A 버튼:** 현재 재생 위치를 시작점(A)으로 설정
- **B 버튼:** 현재 위치를 종료점(B)으로 설정 (A보다 뒤여야 함)
- **CLR 버튼:** A-B 구간 해제
- 프로그레스 바에 A-B 구간이 시각적으로 표시됨 (파란선 = A, 주황선/영역 = B)
- B 지점 도달 시 자동으로 A 지점으로 이동

---

## 4. 재생 모드

- **한 곡 반복 (1🔂):** 현재 곡 무한 반복
- **전체 반복 (🔁):** 마지막 곡 이후 첫 곡으로 돌아감
- **셔플 (🔀):** 랜덤 재생, 이전 트랙 히스토리 유지
- 한 곡 반복 활성화 시 전체 반복/셔플 자동 해제

---

## 5. 볼륨 제어

- **슬라이더:** 0% ~ 100%
- **뮤트 토글:** 🔊/🔇 버튼으로 음소거 전환
- 앱 시작 시 시스템 볼륨과 동기화

---

## 6. 재생목록 관리

- **파일 추가 (+):** 파일 피커로 오디오 파일 선택 (복수 선택 가능)
- **트랙 제거:** 스와이프 삭제 또는 선택 후 🗑️ 버튼
- **저장 (💾):** 현재 재생목록을 이름 지정하여 JSON으로 저장
- **불러오기 (📂):** 저장된 재생목록 목록에서 선택하여 로드
- **탭/더블탭:** 탭으로 선택, 더블탭으로 재생

---

## 7. 가사 표시/편집

곡별로 가사를 입력하고 저장.

- **Edit/Done 전환:** 읽기 전용 ↔ 편집 모드 전환
- **폰트 크기:** A-/A+ 버튼으로 8pt ~ 48pt 조절
- **자동 표시:** 가사가 있는 곡 재생 시 가사 패널 자동 전환
- **저장 위치:** `AppData/Lyrics/{파일명}_{MD5}.txt`
- MP3 태그의 가사도 자동 로드 (별도 파일이 없는 경우)

---

## 8. YouTube 다운로드

YoutubeExplode 라이브러리를 사용한 YouTube 오디오 다운로드.

- YouTube URL 입력 또는 클립보드 자동 감지
- 최고 비트레이트 오디오 스트림 자동 선택
- 다운로드 진행률 표시
- 완료 시 재생목록에 자동 추가
- **공유 인텐트 지원:** 다른 앱에서 YouTube URL 공유 시 즉시 다운로드 시작
- **클립보드 감지:** 앱 실행 시 클립보드에 YouTube URL이 있으면 다운로드 제안

---

## 9. AI 반주 추출 (Demucs)

Demucs AI 모델을 사용한 보컬/반주 분리.

- **모델:** `ggml-model-htdemucs-4s-f16.bin` (81MB, HuggingFace에서 자동 다운로드)
- **처리:** 오디오 → PCM 디코딩 → Demucs 추론 → WAV → MP3 변환
- **포그라운드 서비스:** 장시간 작업을 위한 Android 포그라운드 서비스로 실행
- **진행률:** 실시간 진행률 표시 및 알림바 업데이트
- **취소:** 작업 중 취소 가능
- 완료된 반주 파일은 재생목록에서 원본을 대체

---

## 10. MP3 내보내기

현재 재생 중인 곡을 피치/템포 설정이 적용된 상태로 MP3로 변환.

- SoundTouch + LAME 인코더 사용 (`libjip_export.so`)
- 출력 파일명에 피치/템포 정보 자동 포함 (예: `곡명_P+2_T120.mp3`)
- 저장 위치: 기기 Music 폴더 (권한 불필요)
- 완료 후 공유 인텐트로 다른 앱에 전달 가능
- **📂 버튼:** Music 폴더를 파일 관리자에서 열기

---

## 11. 다운로드 파일 관리

- **다운로드 관리 페이지:** + 버튼 클릭 시 다운로드 파일이 있으면 모달로 표시
- **정리 기능:** 재생목록에 없는 다운로드 파일을 일괄 삭제 (용량 표시 포함)

---

## 12. 레이아웃

기기 방향에 따라 자동 전환.

- **세로 (Narrow):** 왼쪽 재생목록 + 오른쪽 탭(가사/유튜브), 하단 컨트롤
- **가로 (Wide):** 3열 (재생목록 | 가사/유튜브 탭 | 컨트롤)
- 방향 전환 시 120ms 디바운스 적용

---

## 13. 데이터 저장

모든 데이터는 JSON 또는 텍스트 파일로 앱 내부 저장소에 보관. DB 미사용.

| 데이터 | 경로 | 형식 |
|--------|------|------|
| 앱 설정 | `settings.json` | JSON |
| 재생목록 | `Playlists/*.json` | JSON |
| 가사 | `Lyrics/{name}_{md5}.txt` | TXT |
| 다운로드 | `Downloads/` | 오디오 파일 |
| AI 모델 | `Models/` | 바이너리 |
| 추출 결과 | `Stems/{hash}/` | WAV |

---

## 14. 다국어

- 한국어/영어 전환 지원
- 설정에 저장되어 앱 재시작 시 유지

---

## 기술 스택

| 구분 | 기술 |
|------|------|
| 프레임워크 | .NET MAUI 9.0 |
| 재생 엔진 | ExoPlayer (AndroidX Media3) |
| 피치/템포 | SoundTouch (C++ JNI) |
| MP3 인코딩 | LAME (C++ JNI) |
| AI 분리 | Demucs (C++ JNI) |
| YouTube | YoutubeExplode |
| 메타데이터 | TagLibSharp |
| MVVM | CommunityToolkit.Mvvm |
